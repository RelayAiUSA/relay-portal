node_modules/
.netlify/
package-lock.json
relay-pwa/functions/package-lock.json

# Stale build copy that was being served publicly from the publish dir
relay-pwa/sessions/
# Superseded CommonJS functions + the ignored root netlify.toml
/netlify/
/netlify.toml
# Stray deploy archives
deploy-*.zip

# Never read: Netlify base dir is relay-pwa, so only relay-pwa/.netlifyignore applies
/.netlifyignore

# Auto-deploy plumbing (local only — never belongs in the repo)
.deploy-trigger
deploy-log.txt
